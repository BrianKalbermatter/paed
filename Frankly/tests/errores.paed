// Fixture de errores: cada linea de PROCESO tiene exactamente un error.
// Sirve para verificar que el parser NUNCA ignora en silencio.
ACCION errores ES
    AMBIENTE
        cubo1: CUBO;
    PROCESO
        CUBO(nombre = cubo1, posicion = (0,0,0), color = #ff0000);
        MOVER(cubo1, posicion = (1,1,1));
        MOVERR(nombre = cubo1, posicion = (1,1,1));
        COLOR(nombre = cubo1, valor = #00ff00);
        MOVER(nombre = cubo1, posicion = 1,1,1);
        ESCALAR(nombre = cubo1)
        MOVER(nombre = fantasma, posicion = (2,2,2));
FIN_ACCION

// ── LIBRERIA escena
// Este test usa MOVER y COLOR, que NO son del lenguaje: son de la libreria de
// escena de VimMon. Sin declararla, el parser diria "procedimiento desconocido"
// para TODOS y el test dejaria de probar lo que vino a probar.

// ── SALIDA ESPERADA ─────────────────────────────────────────────────────────
// Frankly/tests/errores.paed:8: error: argumento 'cubo1' sin 'clave = valor' en MOVER: la referencia siempre se escribe con nombre
// Frankly/tests/errores.paed:8: error: falta el parametro obligatorio 'nombre' en MOVER
// Frankly/tests/errores.paed:9: error: procedimiento desconocido 'MOVERR' (no esta ni en sintaxis.json ni en escena.json)
// Frankly/tests/errores.paed:10: error: parametro 'valor' no existe en COLOR
// Frankly/tests/errores.paed:10: error: falta el parametro obligatorio 'color' en COLOR
// Frankly/tests/errores.paed:11: error: '1' no es un valor VEC3 valido para posicion de MOVER
// Frankly/tests/errores.paed:11: error: argumento '1' sin 'clave = valor' en MOVER: la referencia siempre se escribe con nombre
// Frankly/tests/errores.paed:11: error: argumento '1' sin 'clave = valor' en MOVER: la referencia siempre se escribe con nombre
// Frankly/tests/errores.paed:11: error: falta el parametro obligatorio 'posicion' en MOVER
// Frankly/tests/errores.paed:12: error: falta ';' al final de la instruccion
