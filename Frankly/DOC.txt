PAED - Documentacion del Compilador/Interprete
================================================
Lenguaje: .paed (pseudo + aed)
Herramienta: paed (script bash sin extension, ejecutable)


>>> SPEC AUTORITATIVA: docs/PAED.md  (desde 2026-08-10)
-----------------------------------------------------------
La definicion COMPLETA y unica del lenguaje vive ahora en:

    paed/Frankly/docs/PAED.md

Antes estaba en docs/paed_spec.md (v2.0). Ese archivo se
absorbio entero en PAED.md el 2026-08-10 y ya no existe: hay
UN solo documento de sintaxis.

Este DOC.txt sigue siendo valido como guia de Frankly (el
interprete en bash), pero NO es la fuente de verdad de la
sintaxis. Si algo de aca contradice a PAED.md, gana PAED.md.

Por que una sola fuente:
  Antes habia DOS lenguajes llamados PAED sin relacion, y
  TRES listas de keywords desincronizadas (sintaxis.json,
  paed.tmLanguage.json, editorText.c). A ninguna le entraban
  FIN_REGISTRO, SUBACCION, FDA, NFDA, Verdadero, Falso ni nil.
  Ese es exactamente el bug que mato a la version anterior.

Que dice PAED.md sobre Frankly:
  - Frankly NO es autoridad de sintaxis. Es implementacion, y
    es PERMISIVO DE MAS: acepta "PARA ... a ..." y comillas
    dobles, y ninguna de las dos es de catedra.
  - NO hay contrato de compatibilidad. Si Frankly acepta algo
    que la catedra no define, el parser en C lo rechaza y el
    que esta mal es Frankly.
  - De Frankly se heredan solo decisiones de ARQUITECTURA, que
    no son sintaxis y no necesitan autoridad de catedra:
      * analizar() antes de ejecutar()        DOC.txt:8-15
      * error "archivo:linea: error: msg"     paed:96,147,155
      * pila para validar anidamiento         paed:110-195

Cambio 2026-08-07: Lua removido de Frankly  (REVERTIDO)
--------------------------------------------------------
Se habia sacado syntax/paed.lua (94 lineas) a _void/, dejando
Frankly 100% bash + JSON.

Volvio el 2026-08-10 y vive en:

    paed/Frankly/syntax/paed.lua

O sea que SI hay resaltado de .paed en Neovim, y
data/sintaxis.json SI tiene consumidor: paed.lua es el que lo
lee. ftdetect/paed.vim tampoco quedo huerfano.

Lo que sigue valiendo de aquel cambio:
  - tmLanguage.json es formato TextMate (VSCode/Zed), no sirve
    para Neovim. Son dos resaltadores distintos para lo mismo,
    y hay que mantenerlos sincronizados a mano.


Pendiente (ver PAED.md seccion 11):
  18 conflictos documentados entre este DOC.txt, la wiki, los
  apuntes y los PDF de catedra. Dos son BLOQUEANTES:
    - Cierre de bloque: FIN_ACCION vs FIN ACCION vs FinAccion
    - Separador del PARA: "a" (repo) vs "hasta" (catedra)

Que es paed?
------------
Es un interprete con fase de analisis previa.
Primero analiza TODO el archivo buscando errores de sintaxis.
Solo si no hay errores, ejecuta el codigo.
Esto lo diferencia de un interprete comun (como python) que explota en runtime.

Dos fases:
  1. analizar()  - recorre el archivo completo, valida estructura
  2. ejecutar()  - solo corre si analizar() no dio errores (return 0)


Sintaxis del lenguaje
----------------------
NO esta documentada aca. Este archivo es el diario de aprendizaje del
interprete; la sintaxis vive en UN SOLO lugar:

  docs/PAED.md          la spec para leer
  data/sintaxis.json    la definicion formal para las herramientas

Todo lo que necesite palabras clave se genera desde sintaxis.json con
tools/generar.sh. Nunca copiar una lista de keywords a mano.


Lo avanzado hoy (2026-03-07)
-----------------------------
- Creada la estructura Compilador/
    Compilador/
    - paed         (el interprete)
    - tests/       (archivos .paed de prueba)
    - output/      (futuro: resultados)
    - DOC.txt      (esta documentacion)

- Creado tests/hola_mundo.paed (primer archivo de prueba)

- paed tiene:
    - Verificacion de argumento vacio         ([ -z ])
    - Verificacion de archivo existente       ([ ! -f ])
    - Verificacion de extension .paed         (${ARCHIVO##*.})
    - while read linea con case para detectar palabras clave:
        ACCION*ES, FIN_ACCION, AMBIENTE, PROCESO

- Conceptos bash aprendidos hoy:
    $1             primer argumento del script
    [ -z ]         verifica si variable esta vacia
    [ ! -f ]       verifica si archivo NO existe
    ${VAR##*.}     extrae extension del nombre de archivo
    return 1       devuelve error y para la funcion
    $?             codigo de salida del ultimo comando
    while read     leer archivo linea por linea
    case/esac      comparar variable contra patrones
    PATRON*)       el * matchea cualquier cosa despues


Tareas pendientes
------------------
[ ] Agregar flags/contadores en analizar():
      tiene_accion=0
      tiene_fin=0
      tiene_ambiente=0
      tiene_proceso=0
    Y verificar al final del while que todos sean 1.
    Si falta alguno -> Error con mensaje claro.

[ ] Detectar numero de linea del error
    Agregar contador: linea_num=0, incrementar en cada iteracion
    Imprimir: "Error linea $linea_num: falta FIN_ACCION"

[ ] Verificar que AMBIENTE venga antes que PROCESO

[ ] Verificar que PROCESO venga antes que FIN_ACCION

[ ] Verificar declaraciones de variables en AMBIENTE
    Formato esperado:  nombre: TIPO;

[ ] Implementar ejecutar()
    - Asignaciones  :=
    - ESCRIBIR()
    - LEER()

[ ] Conectar paed al juego PseudoGames (desde C con system() o popen())


Referencias bash
-----------------
  man bash     secciones: Variables, Conditionals, Loops, Functions
  man sed      secciones: The s command
  man awk      secciones: Pattern-Action, Fields

  Guias online:
  https://www.gnu.org/software/bash/manual/
  https://tldp.org/LDP/abs/html/
  https://www.gnu.org/software/sed/manual/sed.html
  https://www.gnu.org/software/gawk/manual/
